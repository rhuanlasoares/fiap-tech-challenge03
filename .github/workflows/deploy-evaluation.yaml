name: 🔐 Pipeline — evaluation-service (Go)

on:
  push:
    branches: [main]
    # paths: ["evaluation-service/**"]
  pull_request:
    branches: [main]
    # paths: ["evaluation-service/**"]
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  issues: write
  security-events: write

env:
  SERVICE: evaluation-service
  LANGUAGE: golang
  GO_VERSION: "1.25"
  IMAGE_TAG: ${{ github.sha }}

jobs:
  lint:
    name: 🔍 Lint
    uses: ./.github/workflows/reusable-lint.yaml
    with:
      language: golang
      service_path: evaluation-service
      go_version: "1.25"
      python_version: "3.13.13"
    secrets:
      TOKEN: ${{ secrets.GITHUB_TOKEN }}

  sca:
    name: 📦 SCA
    needs: lint
    uses: ./.github/workflows/reusable-sca.yaml
    with:
      language: golang
      service_path: evaluation-service
      go_version: "1.25"
      block_on_critical: true
    secrets:
      TOKEN: ${{ secrets.GITHUB_TOKEN }}

  sast:
    name: 🔒 SAST
    needs: lint
    uses: ./.github/workflows/reusable-sast.yaml
    with:
      language: golang
      service_path: evaluation-service
      go_version: "1.25"
      block_on_critical: true

    secrets:
      TOKEN: ${{ secrets.GITHUB_TOKEN }}
  container-scan:
    name: 🐳 Container Scan & Push GAR
    needs: [sca, sast]
    uses: ./.github/workflows/reusable-container-scan.yaml
    with:
      service_path: evaluation-service
      image_name: evaluation-service
      image_tag: ${{ github.sha }}
      block_on_critical: true
    secrets:
      TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-push-gitops:
    name: 🚀 Build, Push and Update GitOps Manifests
    needs: container-scan
    uses: ./.github/workflows/build-push-gitops.yaml
    with:
      imageName: southamerica-east1-docker.pkg.dev/ces-igniteprogram/artreg-evaluation/evaluation-service
      imageTag: ${{ github.sha }}
      nameService: evaluation-service
      region: southamerica-east1
      service_path: evaluation-service
    secrets:
      PROJECT_ID: ces-igniteprogram
      WORKLOAD_IDENTITY_PROVIDER: ${{ secrets.WORKLOAD_IDENTITY_PROVIDER }}
      SERVICE_ACCOUNT: ${{ secrets.SERVICE_ACCOUNT }}