apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: flag-service-hc-policy
  namespace: flag-ns
spec:
  default:
    checkIntervalSec: 5
    timeoutSec: 5
    healthyThreshold: 2
    unhealthyThreshold: 3
    config:
      type: HTTP
      httpHealthCheck:
        port: 8002
        requestPath: /health
  targetRef:
    group: ""
    kind: Service
    name: flag-service
