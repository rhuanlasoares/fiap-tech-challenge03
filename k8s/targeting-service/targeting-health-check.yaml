apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: targeting-service-hc-policy
  namespace: targeting-ns
spec:
  default:
    checkIntervalSec: 5
    timeoutSec: 5
    healthyThreshold: 2
    unhealthyThreshold: 3
    config:
      type: HTTP
      httpHealthCheck:
        port: 8003
        requestPath: /health
  targetRef:
    group: ""
    kind: Service
    name: targeting-service
